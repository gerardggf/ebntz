enum FirebaseResponse {
  success,
  failure,
}

//opciones de cada publicación
enum PostOptions {
  delete,
  edit,
  info,
}

enum OrderPostsBy {
  creationDate,
  firstDate,
  name,
  location,
}
